/**
 * Re-export from ThemeContext for backward compatibility.
 * Components that import from here will get the live context-aware theme.
 */
export { useTheme } from '../contexts/ThemeContext';
export default undefined;
